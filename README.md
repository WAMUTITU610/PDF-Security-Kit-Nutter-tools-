# PDF-Security-Kit-Nutter-tools-
A professional PDF static analysis tool that inspects suspicious files safely — without executing them. It extracts hashes, detects embedded JavaScript, scores entropy, flags malicious keywords, pulls IOCs, and outputs a colour-coded risk verdict plus optional JSON report.
